{
  "name": "~TODO~",
  "version": "0.0.1",
  "scripts": {
    "archive" : "python3 archive.py",
    "dev": "pnpm run archive && svelte-kit dev",
    "build": "pnpm run archive && svelte-kit build",
    "publish" : "pip install python-frontmatter && python3 archive.py && yarn && yarn run build",
    "preview": "svelte-kit preview"
  },
  "devDependencies": {
    "@sveltejs/kit": "next",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "@sveltejs/adapter-static": "next",
    "backblaze-b2": "^1.7.0",
    "marked": "^3.0.0"
  }
}